# WP17 Exact Changelog — Exercise 4: Classification with Logistic Regression

## Files created

### Data / model layer (Python)
- `scripts/classification_model_audit.py` — the one honest logistic-regression
  audit (fixed split/model, no search); writes the committed result JSON.
- `scripts/classification_model_audit_result.json` — committed audit result:
  confusion matrix, accuracy 0.546, AUC 0.569, cohort/split/protocol detail.
- `scripts/export_classification_threshold_data.py` — exports the fixed
  test-set labels + predicted probabilities for Activity A.
- `scripts/export_classification_imbalance_data.py` — resamples 6 class
  ratios x 5 split seeds x 2 split kinds (60 fits) and exports aggregated
  counts/metrics for Activity B.
- `tests/test_classification_model_audit.py` — 19 tests (committed-result
  validation + isolation tests for leakage, split disjointness, train-only
  fitting, reproducibility).
- `tests/test_export_classification_threshold_data.py` — 8 tests.
- `tests/test_export_classification_imbalance_data.py` — 14 tests (includes
  a synthetic one-class-test-partition case proving undefined AUC is
  handled explicitly).
- `tests/test_exercise_04_notebook.py` — 31 structural/content assertions
  against the canonical notebook.

### Notebook + portable artifacts
- `book/chapters/chapter_04/exercise_04.ipynb` — the canonical Exercise 4
  notebook (30 cells, executed once with real outputs).
- `book/downloads/chapter_04/exercise_04_portable.ipynb` — generated by
  `scripts/build_portable_notebook.py --write --notebook chapter_04`
  (intentionally tracked, same convention as chapters 1-3's portable
  notebooks).

### Data exports (generated, intentionally tracked — same convention as the
existing `abide_regression_models.json` / `abide_knn_*` artifacts)
- `book/_static/widgets/data/abide_classification_threshold.json`
- `book/_static/widgets/data/abide_classification_imbalance.json`

### Activity configs (intentionally tracked, same convention as existing configs)
- `book/_static/widgets/configs/classification_threshold.json`
- `book/_static/widgets/configs/classification_imbalance.json`

### Frontend (TypeScript)
- `interactive/src/classification-metrics.ts` — pure confusion
  matrix/ROC/AUC logic, DOM-free.
- `interactive/src/classification-threshold-data.ts` — data parser for
  Activity A.
- `interactive/src/classification-imbalance-data.ts` — data parser for
  Activity B.
- `interactive/src/components/classification-threshold.ts` — Activity A
  component (mount/draw, Plotly ROC panel via the shared WP16 policy).
- `interactive/src/components/classification-imbalance.ts` — Activity B
  component (mount/draw, HTML-table confusion matrices).
- `interactive/tests/classification-metrics.test.ts` — 14 Vitest tests.
- `interactive/tests/classification-threshold-data.test.ts` — 6 Vitest tests.
- `interactive/tests/classification-imbalance-data.test.ts` — 6 Vitest tests.
- `interactive/e2e/classification-threshold.spec.ts` — 9 standalone
  Playwright tests x 2 base paths = 18 test instances.
- `interactive/e2e/classification-imbalance.spec.ts` — 6 standalone
  Playwright tests x 2 base paths = 12 test instances.
- `interactive/e2e-book/chapter04.spec.ts` — 6 built-book Playwright tests.

## Files modified

- `book/config/abide_modeling.json` — added the `classification` section
  (target/positive/negative class, canonical recipe reuse, holdout split
  note, model spec, threshold/imbalance activity config including the
  N=400 cohort-size rationale and per-ratio/seed determinism).
- `book/_toc.yml` — added `chapters/chapter_04/exercise_04` after chapter 3.
- `book/_static/launch-buttons.js` — added the `chapter_04` entry to
  `PAGE_TO_PORTABLE`.
- `scripts/build_portable_notebook.py` — added `PUBLISHED_PAGE_CH4`,
  `CHAPTER_04` (`NotebookSpec`), added it to the `NOTEBOOKS` dict, fixed a
  stale docstring line that omitted chapter_03/04 from the `--notebook`
  choices list.
- `scripts/smoke_portable_notebook.py` — added a `chapter_04` entry to the
  `SMOKE` dict + a docstring line.
- `tests/test_book_structure.py` — extended
  `test_exercises_two_and_three_follow_exercise_one_in_order` (renamed
  `test_exercises_two_through_four_follow_exercise_one_in_order`) to a
  4-element list and existence check.
- `interactive/src/config.ts` — added `classificationThresholdConfig` and
  `classificationImbalanceConfig` schemas + their exported types, added
  both to the `activityConfigSchema` discriminated union.
- `interactive/src/components/registry.ts` — registered
  `classificationThresholdComponent` and `classificationImbalanceComponent`.
- `interactive/src/styles.css` — added `.widget-confusion-matrix` table
  styling (reuses existing `--fg`/`--border`/`--surface-alt` tokens; no new
  tokens introduced).
- `interactive/e2e-book/launch-buttons.spec.ts` — added the "Chapter 4"
  entry to the `CHAPTERS` array.

## Files not created, renamed, or deleted

No existing file was renamed or deleted. No numbered/duplicate report files
were created.

## Commits created

1. `f90a727` — `WP17: add WP document (checkpoint before implementation)`
   (on `feature/exercise-04-classification`, before this session's
   implementation work; adds only `WPs/WP17_EXERCISE_04_CLASSIFICATION.md`).
2. `ed7d053` — `WP17: Exercise 4 classification notebook, threshold + imbalance activities`
   (on `feature/exercise-04-classification`; the full implementation, 35
   files, 6377 insertions).
3. `2da4086` — `Merge feature/exercise-04-classification into main (WP17)`
   (`--no-ff` merge commit on local `main`).
4. A fourth commit, made immediately after this file is written, adding
   `WPs/reports/WP17_REPORT.md` and this changelog to local `main`. Its SHA
   is reported only in Claude's terminal response (not written back into
   either report file, per WP17 §11).

## Generated artifacts and whether they are intentionally tracked

- `book/_static/widgets/app/` — Vite build output. **Not tracked**
  (`.gitignore`: `book/_static/widgets/app/`), same as every prior chapter's
  widget bundle.
- `book/_build/` — Jupyter Book build output. **Not tracked**
  (`.gitignore`: `book/_build/`).
- `book/_static/widgets/data/abide_classification_threshold.json` and
  `abide_classification_imbalance.json` — generated by the two new
  `scripts/export_classification_*.py` scripts. **Intentionally tracked**,
  matching the existing convention for `abide_regression_models.json` /
  `abide_knn_abc_manifest.json` etc.
- `book/downloads/chapter_04/exercise_04_portable.ipynb` — generated by
  `scripts/build_portable_notebook.py`. **Intentionally tracked**, matching
  chapters 1-3.
- `scripts/classification_model_audit_result.json` — generated by
  `scripts/classification_model_audit.py --run`. **Intentionally tracked**,
  matching `scripts/knn_model_audit_result.json` /
  `scripts/regression_model_audit_result.json`.

## Confirmation

No screenshots, videos, temporary Playwright configs, generated caches,
`.DS_Store` files, or virtual environments were created or committed. No
remote operations (push, deploy, GitHub Actions) were performed in WP17.
WP18 was not started.
