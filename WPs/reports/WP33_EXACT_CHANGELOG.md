# WP33 Exact Changelog

Every file added, modified, or removed by WP33 (Exercise 8: Unsupervised
Learning), relative to the starting branch tip
`feature/wp32-exercise7-gradient-boosting` @ `73d802b`.

## Specification checkpoint (already committed before implementation)

- **`WPs/WP33_EXERCISE_8_UNSUPERVISED_LEARNING.md`** (added) -- the WP33
  specification, committed as the initial checkpoint (`d76437d`) before any
  implementation began.

## Manifest

- **`book/config/abide_modeling.json`** (modified) -- added the
  `unsupervised` top-level key: the 360-feature recipe (identical to every
  other exercise), the standardization rule, the anatomical groups used for
  the grouped-loading summary (reusing the existing `bundles` entries), the
  loading-summary parameters, the synthetic projection-activity generating
  process, the K-means catalogue grid (`retainedPcGrid`, `kGrid`, `seeds`,
  `n_init`, defaults, external-variable labels), and the supervised-pipeline
  settings (`component_grid`, `dev_split`/CV settings). Verified
  self-consistent by `scripts/abide_modeling_data.py --check`.

## Notebook

- **`book/chapters/chapter_08/exercise_08.md`** (removed) -- the WP25/WP32
  placeholder page, replaced by the real notebook below.
- **`book/chapters/chapter_08/exercise_08.ipynb`** (added) -- the canonical
  Exercise 8 notebook, 33 cells, executed in place (see WP33_REPORT.md §4
  for the exact section list). No `_toc.yml` change was needed -- it already
  referenced `chapters/chapter_08/exercise_08` extensionlessly.
- **`book/downloads/chapter_08/exercise_08_portable.ipynb`** (added,
  generated) -- the portable Colab/VS Code notebook, generated by
  `scripts/build_portable_notebook.py --write --notebook chapter_08` and
  smoke-executed outside the repository tree with zero errors.

## Portable-notebook generator

- **`scripts/build_portable_notebook.py`** (modified) -- added
  `PUBLISHED_PAGE_CH8`, `_CH8_BANNER`, `_CH8_SETUP`,
  `_CH8_PROJECTION_IFRAME_REPLACEMENT`, `_CH8_KMEANS_IFRAME_REPLACEMENT`, and
  the `CHAPTER_08` `NotebookSpec`; registered it in `NOTEBOOKS`; updated the
  module docstring's `--notebook` choices list and the "Exercises 8-12 are
  placeholders" note to "Exercises 9-12."

## Portable-notebook smoke registry

- **`scripts/smoke_portable_notebook.py`** (modified) -- extended the
  `SMOKE` registry (previously chapters 1-4 only, a known gap WP32's own
  report flagged and left unaddressed) with entries for `chapter_05`,
  `chapter_06`, `chapter_07`, and `chapter_08`, each with a handful of
  deterministic expected-output strings pulled from that chapter's own
  already-executed canonical-notebook output. Updated the module docstring
  to describe all four new entries. Verified: each new chapter individually
  (`--notebook chapter_0N`) and the full unmodified `chapter_01`-`chapter_04`
  set together in one run -- all 8 chapters pass.

## Audit script (new)

- **`scripts/pca_kmeans_audit.py`** (added) -- `--run`/`--check` audit:
  cohort/feature counts, standardized PCA explained-variance ratios and
  cumulative variance (50 components), top-ROI loadings and grouped
  mean-absolute loading per anatomical bundle, and the supervised PCA
  pipeline (component-grid CV, selection, locked-test evaluation, baseline
  comparison).
- **`scripts/pca_kmeans_audit_result.json`** (added, generated) -- the
  committed audit result (`pca_kmeans_audit.py --run`), re-validated
  offline (`--check`).

## Widget export scripts (new)

- **`scripts/export_pca_projection_widget.py`** (added) -- `--refresh`/
  `--check`: the fixed synthetic 40-point 2-D cloud and the true PC1
  direction/explained-variance-ratio for Activity A's reveal control.
- **`scripts/export_pca_kmeans_widget.py`** (added) -- `--refresh`/`--check`:
  the real-cohort PCA fit (PC1/PC2 scores, stored once) and the full
  75-combination K-means catalogue for Activity B. `--check` validates
  structure + canonical serialization offline (the artifact's own source
  requires network to reload, matching `export_regression_catalog.py`'s
  convention).

## Widget configs and data artifacts (new)

- **`book/_static/widgets/configs/pca_projection.json`** (added)
- **`book/_static/widgets/configs/pca_kmeans_explorer.json`** (added)
- **`book/_static/widgets/data/pca_projection.json`** (added, generated)
- **`book/_static/widgets/data/pca_kmeans_explorer.json`** (added, generated;
  940.9 KiB uncompressed / 49.3 KiB gzip -9)

## Interactive runtime (TypeScript)

- **`interactive/src/config.ts`** (modified) -- added the `pca-projection`
  and `pca-kmeans-explorer` config schemas (including the
  `pcaKmeansExternalVariable` enum) and registered both in the
  discriminated config union; exported their inferred types.
- **`interactive/src/components/registry.ts`** (modified) -- imported and
  registered `pcaProjectionComponent` and `pcaKmeansExplorerComponent`.
- **`interactive/src/pca-projection-data.ts`** (added) -- Zod schema +
  `projectAtAngle()` (pure trigonometry: captured variance / reconstruction
  MSE for any angle).
- **`interactive/src/components/pca-projection.ts`** (added) -- Activity A's
  DOM/Plotly mount: angle slider, reveal/reset, scatter with axis line,
  residual segments, and projected points.
- **`interactive/src/pca-kmeans-explorer-data.ts`** (added) -- Zod schema +
  `catalogEntryFor()`/`externalValuesFor()`/`catalogKey()`.
- **`interactive/src/pca-kmeans-explorer.ts`** (added) -- pure display logic
  (`clusterColor`, `sharedAxisRange`, `indicesForCluster`,
  `categoryShareByCluster`, `siteShareMatrix`, `agesByCluster`), split out
  from the DOM component so it is independently unit-testable, matching this
  project's `<name>.ts` (pure logic) + `<name>-data.ts` (schema) +
  `components/<name>.ts` (DOM) convention (e.g.
  `regularization-explore.ts`).
- **`interactive/src/components/pca-kmeans-explorer.ts`** (added) --
  Activity B's DOM/Plotly mount: 4 selects, 5 panels (scatter+centers,
  inertia, silhouette, sizes, composition).

## Frontend tests (new)

- **`interactive/tests/pca-projection-data.test.ts`** (added) -- schema +
  `projectAtAngle` math (8 tests).
- **`interactive/tests/pca-kmeans-explorer-data.test.ts`** (added) -- schema
  + catalogue-lookup helpers (8 tests).
- **`interactive/tests/pca-kmeans-explorer.test.ts`** (added) -- pure
  display-logic functions (10 tests).

## Playwright specs (new / modified)

- **`interactive/e2e/pca-projection.spec.ts`** (added) -- standalone
  Activity A checks: defaults, angle-driven redraw, the
  captured-variance+MSE=total invariant, reveal/reset, narrow viewport, and
  the missing-config error panel.
- **`interactive/e2e/pca-kmeans-explorer.spec.ts`** (added) -- standalone
  Activity B checks: defaults, the "uses every retained component" note,
  k/retained-PC/seed control wiring, composition-chart-type switching
  (heatmap for site, box for age), narrow viewport, and the missing-config
  error panel.
- **`interactive/e2e/plot-visual-policy.spec.ts`** (modified) -- added 3
  chart entries (`pca-projection`, `pca-kmeans-explorer` scatter,
  `pca-kmeans-explorer` composition) to the shared presentation-policy
  regression guard.
- **`interactive/e2e-book/chapter08.spec.ts`** (added) -- built-book
  structural + interaction checks for both new activities (title, iframe
  count, scope exclusions, Colab button, control wiring, refresh-restores-
  defaults, narrow viewport).
- **`interactive/e2e-book/chapter08-dark-mode.spec.ts`** (added) -- dedicated
  built-book dark-mode regression guard for both new activities
  (light -> dark -> reload-in-dark -> light), following the
  `chapter01-dark-mode.spec.ts` template (the gap WP32's own report flagged
  as not done for Exercise 7 is closed here for Exercise 8).
- **`interactive/e2e-book/launch-buttons.spec.ts`** (modified) -- added a
  "Chapter 8" case to `CHAPTERS`; moved the "placeholder gets no Colab
  button" assertion from Exercise 8 to Exercise 9.

## Launch-button registration

- **`book/_static/launch-buttons.js`** (modified) -- registered
  `chapters/chapter_08/exercise_08.html` -> the new portable notebook;
  updated the "placeholder Exercises 8-12" comment to "9-12."

## Python tests (new / modified)

- **`tests/test_exercise_08_notebook.py`** (added) -- 46 tests: opening
  structure, data loading/terminology, both activities' iframes and hidden
  reproduction/demo cells, PCA section content and audited-number matching,
  clustering-concept content, research-example workflow and caution text,
  supervised-pipeline structure/leakage-safety/audited-number matching,
  scope exclusions, summary, launch-button/portable-notebook registration.
- **`tests/test_pca_kmeans_audit.py`** (added) -- 14 tests re-validating the
  committed audit result.
- **`tests/test_export_pca_projection_widget_data.py`** (added) -- 11 tests,
  including independent recomputation of the projection math and a
  local-variance-maximum check on the true PC1 angle.
- **`tests/test_export_pca_kmeans_widget_data.py`** (added) -- 12 tests,
  including full-grid combination coverage, cluster-size/label consistency,
  and the PC1/PC2-stored-once invariant.
- **`tests/test_book_structure.py`** (modified) -- extended the
  `.ipynb`-exists range to include chapter 8 and the `.md`-placeholder range
  to start at 9.
- **`tests/test_exercise_04_notebook.py`** (modified) -- the "placeholder,
  no launch button" range moved from `range(8, 13)` to `range(9, 13)`.
- **`tests/test_exercise_06_notebook.py`** (modified) -- the "no portable
  notebook" range moved from `range(8, 13)` to `range(9, 13)`.
- **`tests/test_exercise_07_notebook.py`** (modified) -- the "no launch
  button" range moved from `range(8, 13)` to `range(9, 13)`.
- **`tests/test_placeholder_exercises.py`** (modified) -- removed exercise 8
  from `PLACEHOLDER_TITLES` (now 9-12) and updated the module docstring.
- **`tests/test_wp25_content_audit.py`** (modified) -- corrected
  `EXERCISE_TITLES[8]` from the stale `"Exercise 8: PCA and Clustering"` to
  the required `"Exercise 8: Unsupervised Learning"`; updated `_title_of`'s
  `.ipynb`-vs-`.md` cutoff from `n <= 7` to `n <= 8`; extended the
  final-project-reference and no-duplicate-copy checks' chapter ranges to
  include chapter 8.

## Reports

- **`WPs/reports/WP33_REPORT.md`** (added) -- this report.
- **`WPs/reports/WP33_EXACT_CHANGELOG.md`** (added) -- this file.

## Untouched (confirmed)

- `book/syllabus.md` -- not opened or modified.
- `course_overview/` (Word course-overview document) -- not opened or
  modified.
- `book/_toc.yml` -- not modified (already referenced chapter 8
  extensionlessly).
- `WPs/reports/WP16_ARCHITECT_REPORT.md`,
  `WPs/reports/WP21_DEPLOYMENT_REPORT.md` -- preserved untracked, as
  required by WP33 §2.4.
