# WP27R Exact Changelog

Diff base: `8d48bca` (`feature/wp27-validation-cross-validation`, the WP27
placeholder-fix commit this WP started from).
Diff head: `83f3f3a` (`WP27R: densify k grids, reveal third test-MSE panel,
replace nested-CV diagram`), plus this reports commit on top.

No file was moved or deleted. One file created, fifteen modified.

## Created

- `WPs/WP27R_EXERCISE_4_INTERACTION_AND_NESTED_CV_REFINEMENTS.md` -- this
  WP's specification, committed before implementation (commit `31d5b17`).

## Modified

| File | What changed |
| --- | --- |
| `book/_static/custom.css` | Added `--ml-ncv-outer-train` / `--ml-ncv-outer-test` / `--ml-ncv-inner-train` / `--ml-ncv-inner-val` custom properties: light defaults on `:root`, dark overrides in the existing `html[data-theme="dark"]` block. Used only by the new nested-CV diagram. |
| `book/_static/widgets/configs/validation_lock_test.json` | `defaultK` 20 → 25 (the new validation-selected k); added `postRevealReflectionPrompts` (4 new post-reveal questions). |
| `book/_static/widgets/data/wp27_nested_cv_explorer.json` | Regenerated by `scripts/export_wp27_widget_data.py --write` from the re-run audit (denser `NESTED_CANDIDATE_KS`, new per-fold results). |
| `book/_static/widgets/data/wp27_validation_lock_test.json` | Regenerated the same way (denser `CANDIDATE_KS`, new training/validation-selected k and per-k rows). |
| `book/chapters/chapter_04/exercise_04.ipynb` | Section 5 intro reworded to mention the three-panel figure; two new markdown cells after the activity (post-reveal comparison prose, a second "Think again" admonition); new "### Optional: Reproduce the Tuning Activity in Python" heading + one-sentence blurb; the fit/validation cell and the reveal cell tagged `hide-cell` and their `CANDIDATE_KS` densified; Section 6's Mermaid flowchart replaced by the native `ml-ncv-diagram` HTML block + caption; `NESTED_CANDIDATE_KS` densified; the post-results interpretation cell rewritten to report the real (now-varying) per-fold outcome instead of the old "all five folds agreed" claim. Re-executed in place. |
| `book/downloads/chapter_04/exercise_04_portable.ipynb` | Regenerated from the canonical notebook via `scripts/build_portable_notebook.py --write --notebook chapter_04` (no manual edits). |
| `interactive/e2e-book/chapter04.spec.ts` | Updated for `defaultK=25`, the denser grid (asserts `k-12` absent, required k's present), the three-panel trace-count check pre/post lock, the methodology warning, the new diagram (Mermaid absence, `.ml-ncv-diagram` presence and operation labels), the collapsed optional-Python heading, and a diagram-visibility check in the narrow-viewport test. |
| `interactive/e2e-book/wp22-cross-chapter-dark-mode.spec.ts` | Added a check that `.ml-ncv-diagram`'s `--ml-ncv-outer-train` custom property resolves to the dark-palette value once the book's dark toggle is active. |
| `interactive/e2e/validation-lock-test.spec.ts` | Rewritten for the new default k=25, the required dense-grid tabs (and absence of a `12` tab), the 2-trace-before/3-trace-after-lock check, the methodology warning, the post-reveal reflection section, and the reworded reset note. |
| `interactive/src/components/plotly-policy.ts` | `buildPlotLayout` now also applies the shared axis-defaults merge to any `xaxis2`/`xaxis3`/`yaxis2`/`yaxis3` override key (generalized from `xaxis`/`yaxis` only), needed for the lock-test widget's three stacked subplots. |
| `interactive/src/components/validation-lock-test.ts` | Rebuilt around a three-panel (train/validation/test) Plotly figure with matched log x-axes and a shared y-range; the test trace is constructed only when `locked`; added vertical marker shapes for the chosen/training-selected/validation-selected k; added the methodology-warning paragraph and a post-reveal reflection-prompts section; reworded the reset note. |
| `interactive/src/config.ts` | Added optional `postRevealReflectionPrompts: z.array(z.string().min(1)).optional()` to `validationLockTestConfig`. |
| `scripts/smoke_portable_notebook.py` | Updated chapter_04's expected key-value strings to the new validation-selected k=25 / test MSE 32.6 / nested per-fold selection `[15, 12, 10, 18, 15]` / mean outer-test MSE 33.4. |
| `scripts/wp27_validation_audit.py` | `CANDIDATE_KS` densified to `[1, 2, 3, 5, 8, 10, 15, 20, 25, 30, 50, 75, 100, 150, 250, 400, 564]`; `NESTED_CANDIDATE_KS` densified to `[8, 10, 12, 15, 18, 20, 22, 25, 28, 30, 40, 50]`. |
| `scripts/wp27_validation_audit_result.json` | Regenerated via `python3 scripts/wp27_validation_audit.py --run` against the new grids. |
| `tests/test_exercise_04_notebook.py` | Updated two pre-existing assertions to match reworded text (`test_outer_metrics_not_inner_score_presented_as_performance_estimate`, `test_reset_behavior_is_explicit`); added a new `WP27RGridsAndActivity` test class (18 tests) covering the dense grids, the lock-gated third trace, selected-k markers, the methodology warning, post-reveal prompts, the removed Mermaid flowchart and its native replacement, dark/light diagram tokens, the denser nested grid, the honestly-reported per-fold variation, the collapsed optional-Python section, and that `scripts/build_course_overview_docx.py` is untouched. |

## Untouched (confirmed)

- `book/syllabus.md` (checked by the pre-existing `test_syllabus_source_unchanged_since_wp27_base_commit`).
- `scripts/build_course_overview_docx.py` (checked by the new `test_word_course_overview_script_untouched`).
- `book/chapters/chapter_0[1-3]/*`, `book/chapters/chapter_0[5-9]/*`, `book/chapters/chapter_1[0-2]/*`.
- `book/_static/widgets/data/wp27_validation_stability.json` and its config -- Part A of the audit (sample-size stability) does not depend on either candidate-k grid and was correctly left unregenerated (confirmed byte-identical by `scripts/export_wp27_widget_data.py --check`).
- `WPs/reports/WP16_ARCHITECT_REPORT.md`, `WPs/reports/WP21_DEPLOYMENT_REPORT.md` -- pre-existing untracked files, out of scope, left untouched.

## Build artifacts (not committed; `book/_build/` is gitignored)

`jupyter-book build book` was run to validate the diagram and hide-cell
rendering; its output lives under `book/_build/html/` and is not part of
this changelog.
