# WP24 Exact Changelog

Branch: `edit/wp24-notebook-concision`

Commits (in order):

1. `205d53f` — WP24: add notebook concision and content refinement spec (checkpoint;
   added `WPs/WP24_NOTEBOOK_CONCISION_AND_CONTENT_REFINEMENT.md` only).
2. `2e5eb52e74b130cc6f279c9b3063abebbb2d1abf` — WP24: notebook concision and content
   refinement (Exercises 1-4) (implementation; 24 files changed).
3. (this commit) — WP24: add report and exact changelog.

---

## Files created

- `WPs/WP24_NOTEBOOK_CONCISION_AND_CONTENT_REFINEMENT.md` — the WP specification,
  written after investigating the repo (correlation widget, sample-size audit,
  portable-notebook generator, cell-tag conventions, syntax-highlight CSS) and committed
  as a standalone checkpoint before any implementation edit.
- `tests/test_wp24_content_audit.py` — 33 focused regression tests covering: the
  decision-block move (Exercise 1), absence of `imput*` (Exercise 1), absence of
  "Spearman" and absence of a method selector (Exercise 1, correlation widget/maths/
  config/data), single-occurrence checks for the Exercise 2 exploratory-comparison
  warning and the Exercise 4 majority-baseline paragraph, the single retained Exercise 4
  prompt set plus the new comparison question (canonical and portable), the three
  newly-`hide-cell`d reproduction cells and their fully-visible portable counterparts,
  and the dark-mode syntax-color contrast ratio.
- `WPs/reports/WP24_REPORT.md` — this WP's report.
- `WPs/reports/WP24_EXACT_CHANGELOG.md` — this file.

## Files modified

- `NOTEBOOK_AUTHORING_STANDARDS.md` — added §7 "Concision and duplication (WP24)" with
  five durable rules (plain language over jargon, no repeated warnings/explanations
  around one activity, brief action-oriented interactive instructions, `hide-cell` for
  redundant website-only reproductions, portable notebooks keep such reproductions
  visible/runnable, internal notes never reach student-facing text); renumbered the
  following "Before you start a notebook-editing WP" section from §7 to §8.

- `book/_static/custom.css` — added a dark-mode-only override (`html[data-theme="dark"]
  .highlight .c1, .nb, .mi { color: #c7a86b; }`) right after the existing `.cell_input
  div.highlight` rule, with an explanatory comment; no light-mode change.

- `book/_static/widgets/configs/eda_correlation.json` — removed `defaultMethod`;
  reworded `description` and `instructions` to drop the Pearson/Spearman framing;
  reworded the last `reflectionPrompts` entry to drop "Pearson or Spearman".

- `book/_static/widgets/configs/regression_compare.json` — removed the
  `selectionBiasNote` key entirely (duplicate of the notebook's own warning).

- `book/_static/widgets/configs/classification_imbalance.json` — removed the
  `imbalanceNote` key entirely (duplicate of the notebook's own paragraph); added a
  third `reflectionPrompts` entry (the new accuracy-vs-baseline comparison question).

- `book/chapters/chapter_01/exercise_01.ipynb` — moved and reworded the "Our approach in
  this exercise" block to sit before the complete-case retention activity; renamed the
  "Imputing a numerical variable" section and replaced every `imput*` occurrence
  (headings, prose, a table row, two dropdowns, three code cells' variable names/print
  text/column labels) with plain "fill in missing values" language; removed Spearman
  from the opening list, the correlation section heading/table/worked example/prose, the
  "Explore correlations yourself" instructions, and the synthesis-challenge question +
  dropdown answer; changed the histogram-reproduction code cell's tag from `hide-input`
  to `hide-cell`. Re-executed end-to-end (`jupyter nbconvert --execute --inplace`) so
  every stored output matches the new code/wording; no execution errors.

- `book/chapters/chapter_02/exercise_02.ipynb` — rewrote the Section 5 lead-in (cell
  before the sample-size code) to be concise and to accurately describe the predeclared,
  non-ranked feature-bundle selection procedure per `scripts/sample_size_audit.py`, with
  one retained "teaching demonstration" caution; shortened the following "What the
  learning curve shows" admonition. No code, feature, size, seed, or result changed.

- `book/chapters/chapter_04/exercise_04.ipynb` — rewrote cell 28 (Section 6) to contain
  only the single majority-baseline paragraph plus one sentence pointing at the widget's
  own Reflect questions, removing both duplicate "Think first"/"Check your reasoning"
  pairs; added `hide-cell` to the previously-untagged decision-threshold reproduction
  cell (`threshold = 0.50`) and the class-imbalance reproduction cell
  (`class_ratio = "90:10"`).

- `book/downloads/chapter_01/exercise_01_portable.ipynb`,
  `book/downloads/chapter_02/exercise_02_portable.ipynb`,
  `book/downloads/chapter_04/exercise_04_portable.ipynb` — regenerated by
  `scripts/build_portable_notebook.py --write` from the edited canonical notebooks;
  `--check` confirms determinism. (`chapter_03`'s portable notebook was unaffected and
  not rewritten.)

- `interactive/README.md` — removed five Spearman mentions (status blurb, unit-test
  description, the config/component/maths documentation table rows, the activity
  description sentence, and the Pearson/Spearman comparison table — now a Pearson-only
  table).

- `interactive/src/correlation.ts` — removed `spearman()`, `averageRanks()`, and the
  `CorrelationMethod` type; `coefficient()`/`computeCorrelation()` no longer take a
  method argument and always compute Pearson; `CorrelationResult` no longer has a
  `method` field; updated the file-header comment.

- `interactive/src/components/correlation.ts` — removed the method `<select>`
  (`data-testid="correlation-method"`) and its inclusion in the control layout and the
  redraw-on-change listener list; removed the `methodName`/`plot.dataset.activeMethod`
  logic, hardcoding "Pearson correlation" in every stats string; updated the
  `computeCorrelation` call site and the file-header comment; removed the unused
  `CorrelationMethod` import.

- `interactive/src/components/regression-compare.ts` — removed the `selectionNote`
  paragraph (`data-testid="regression-selection-bias"`) that rendered the now-removed
  `config.selectionBiasNote`.

- `interactive/src/components/classification-imbalance.ts` — removed the
  `imbalanceNote` paragraph that rendered the now-removed `config.imbalanceNote`.

- `interactive/src/config.ts` — removed `defaultMethod: z.enum(["pearson",
  "spearman"])` from `edaCorrelationConfig`; removed `selectionBiasNote` from
  `regressionCompareConfig`; removed `imbalanceNote` from `classificationImbalanceConfig`.

- `interactive/tests/correlation.test.ts` — removed the `averageRanks`/`spearman`
  imports and their two `describe` blocks; removed the method argument from every
  `computeCorrelation(...)` call and the `r.method` assertion; removed the
  Spearman-specific expectations from the "against the committed abide_retention.json"
  cross-check test (kept the Pearson assertions).

- `interactive/tests/config.test.ts` — removed `defaultMethod` from the
  `validCorrelation` fixture and the "rejects an unknown defaultMethod" test (the field no
  longer exists); removed `selectionBiasNote` from the `validRegressionCompare` fixture.

- `interactive/e2e/correlation.spec.ts` — removed the `[data-testid="correlation-method"]`
  assertions from the defaults test; removed the entire "Pearson <-> Spearman changes the
  displayed coefficient" test; removed the method-selector interaction from the
  "reloading restores defaults" test; replaced the mobile-viewport method-selector
  interaction with an equivalent grouping-selector interaction.

- `interactive/e2e-book/chapter01.spec.ts` — removed the `data-active-method` assertion
  and the method-switch step (`selectOption("spearman")`/`selectOption("pearson")`) from
  the full built-page correlation activity test, keeping the variable-switch and
  grouping-switch coverage.

- `scripts/build_portable_notebook.py` — reworded the `eda-correlation` iframe
  replacement text to drop "switch between Pearson and Spearman"; added an equivalent
  reflection question ("Think about this as you try a few ratios: …") to
  `_CH4_IMBALANCE_IFRAME_REPLACEMENT` so the portable notebook keeps a reachable prompt
  now that the notebook's own duplicate prompt was removed.

- `tests/test_notebook_corrections.py` — updated the correlation-heading assertion to
  "### Pearson correlation between numerical variables"; updated the hide-tag count
  assertion (`hide-input`: 7→6, `hide-cell`: 0→1) and the histogram-cell tag assertion
  (`hide-input`→`hide-cell`) to reflect the intentional §7 change.

## Files explicitly left unchanged (reviewed, out of scope)

- `WPs/reports/WP16_ARCHITECT_REPORT.md`, `WPs/reports/WP21_DEPLOYMENT_REPORT.md` —
  pre-existing untracked files, per the WP's explicit instruction.
- `scripts/build_course_overview_docx.py` and
  `course_overview/Machine_Learning_for_Neuroscience_Notebook_Overview.docx` — still
  reference "Spearman correlations"; left untouched per the standing WP20 policy on this
  file (see report §8).
- `scripts/export_widget_data.py` and `book/config/abide_modeling.json` — each contain
  one internal, non-Exercise-1, non-student-facing "imput*" mention; reviewed and left
  untouched (see report §8).
- `book/chapters/chapter_03/exercise_03.ipynb` and its portable notebook — out of scope
  (no Exercise 3 edits requested).

## Build artifacts (gitignored, not committed)

- `book/_build/` — regenerated once for validation (Jupyter Book build).
- `book/_static/widgets/app/` — regenerated once for validation (`npm run build` in
  `interactive/`); both are covered by `.gitignore` and are rebuilt fresh at deploy time.
