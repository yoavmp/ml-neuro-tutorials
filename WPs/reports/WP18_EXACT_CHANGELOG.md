# WP18 Exact Changelog

Scope: `wp18-start` (`70d39eb`) → local `main` merge commit `8ae920a`.

## Commits

1. `aef17c4` — "WP18: add WP document (checkpoint before implementation)" (branch `revise/exercise-04-classification`, created from `wp18-start`)
2. `570feb0` — "WP18: honest C selection, confusion-matrix labels, misleading-accuracy activity, course overview" (branch `revise/exercise-04-classification`)
3. `8ae920a` — "Merge revise/exercise-04-classification into main (WP18)" (`--no-ff` merge into local `main`)
4. (this report's own commit — SHA reported only in Claude's final chat response, not written back into this file)

## Files created

- `WPs/WP18_EXERCISE_04_REVISIONS_AND_COURSE_OVERVIEW.md` — the WP document, saved verbatim (commit 1)
- `course_overview/Machine_Learning_for_Neuroscience_Notebook_Overview.docx` — Word overview of Exercises 1–4 (commit 2)
- `scripts/build_course_overview_docx.py` — generates the above `.docx` (commit 2)
- `tests/test_course_overview_docx.py` — structural validation of the `.docx` (commit 2)
- `WPs/reports/WP18_REPORT.md` — this WP's narrative report (report commit)
- `WPs/reports/WP18_EXACT_CHANGELOG.md` — this file (report commit)

## Files modified

- `book/_static/widgets/configs/classification_imbalance.json` — title/instructions/`imbalanceNote` rewritten for the new activity; `stratificationNote` field removed
- `book/_static/widgets/data/abide_classification_imbalance.json` — regenerated: `schemaVersion` 1→2, flattened per-(ratio, seed) entries (no more `stratified`/`unstratified` nesting), new `balancedAccuracy`/`sensitivity`/`specificity` fields, `selectedC`/`model` fields added, uses C=0.01 throughout
- `book/_static/widgets/data/abide_classification_threshold.json` — regenerated with C=0.01; `selectedC` field added
- `book/chapters/chapter_04/exercise_04.ipynb` — canonical notebook: 5 new cells (honest-C think-first, "Choosing C honestly" intro, CV-selection code, CV-curve plot, C-explanation/quote) inserted after the split cell; explicit-scaling and pipeline-model cells now use `C_SELECTED` and the explicit-scaling cell ends with `;` (no stored outputs); confusion-matrix cell now labels `TN =`/`FP =`/`FN =`/`TP =`; Section 6 header/intro/iframe-title/explanatory-text/portable-code cell replaced for the imbalance-and-misleading-accuracy activity; Takeaways and the three review questions revised; re-executed end to end
- `book/config/abide_modeling.json` — `classification.model` updated to cite C=0.01 selection; `cv_for_c_selection`, `selected_C`, `selected_C_rationale` fields added; `imbalance_activity.split_seeds_note` rewritten to drop the stratified/unstratified framing
- `book/downloads/chapter_04/exercise_04_portable.ipynb` — regenerated by `scripts/build_portable_notebook.py --write --notebook chapter_04` from the revised canonical notebook (37 cells)
- `interactive/e2e-book/chapter04.spec.ts` — imbalance iframe title selector updated; AUC assertions 0.569→0.593; imbalance test block rewritten for the flattened UI (`cls-imb-plot`/`cls-imb-counts`/`data-baseline-accuracy` instead of stratified/unstratified testids)
- `interactive/e2e/classification-imbalance.spec.ts` — fully rewritten for the new single-chart UI
- `interactive/src/classification-imbalance-data.ts` — zod schema flattened to match the new artifact shape; `schemaVersion` literal 2; `auc` no longer nullable
- `interactive/src/components/classification-imbalance.ts` — fully rewritten: two-bar Plotly chart (model accuracy vs. majority baseline) replacing the two-panel stratified/unstratified table layout; shared WP16 Plotly policy (`plotly-policy.ts`) used throughout
- `interactive/src/config.ts` — `classificationImbalanceConfig.stratificationNote` renamed to `imbalanceNote`
- `interactive/tests/classification-imbalance-data.test.ts` — rewritten for the flattened schema, including a rejection test for the old schemaVersion-1 shape
- `scripts/build_course_overview_docx.py` — n/a (created, listed above)
- `scripts/build_portable_notebook.py` — `_CH4_IMBALANCE_IFRAME_REPLACEMENT` text and its lookup key updated for the renamed iframe title
- `scripts/classification_model_audit.py` — `C_FIXED=1.0` replaced with `C_GRID`/`CV_SPLITS`/`CV_RANDOM_STATE`/`CV_SCORING`; new `_select_c()` (GridSearchCV + StratifiedKFold) and `select_canonical_c()` helpers added; `_pipeline()`/`_fit_eval()` now take an explicit `C` argument; `run_audit()`/`validate()`/`_print_summary()` updated to compute, validate, and report the CV selection
- `scripts/classification_model_audit_result.json` — regenerated: `cv_selection` section added (grid/folds/scoring/curve/selected_C/selected_mean_cv_auc); `protocol.model`/`protocol.note` cite the honest selection; `locked_test_eval` now reflects C=0.01
- `scripts/export_classification_imbalance_data.py` — rewritten: single stratified split per (ratio, seed) using `select_canonical_c()`'s fixed C throughout; `_eval_split()` now also returns `balancedAccuracy`/`sensitivity`/`specificity` and raises on a one-class test partition instead of reporting `auc: null`; `SCHEMA_VERSION` bumped to 2
- `scripts/export_classification_threshold_data.py` — uses `select_canonical_c()` instead of the old fixed `C=1.0`; `selectedC` field added to the artifact
- `scripts/smoke_portable_notebook.py` — chapter_04 expected strings updated (selected C, new accuracy/AUC, new default cohort counts for `class_ratio="90:10"`)
- `tests/test_classification_model_audit.py` — `test_c_is_fixed_at_1_and_not_tuned` replaced with CV-selection tests (`test_c_is_selected_honestly_by_cross_validation`, `test_cv_curve_has_one_row_per_grid_point_and_matches_grid`); `FitEvalIsolation` tests updated to pass `C` explicitly; new `SelectCIsolation` test class added
- `tests/test_exercise_04_notebook.py` — section-6 heading text updated; `test_model_spec_is_fixed_c_and_not_tuned` replaced with `test_c_is_selected_honestly_by_cross_validation` (+ a verbatim-quote test); new tests for the TN/FP/FN/TP labels, the no-rich-estimator-output guarantee, and the absence of stratified/unstratified language; `gridsearchcv` removed from the excluded-topics list; imbalance iframe-title and portable-cell assertions updated
- `tests/test_export_classification_imbalance_data.py` — rewritten for the flattened schema; new tests for `balancedAccuracy`, "no stratified/unstratified nesting", "AUC always defined", and "same selected C used throughout"
- `tests/test_export_classification_threshold_data.py` — one test added confirming `selectedC` is cited and is not the old untuned default

## Files renamed or deleted

None.

## Untouched (explicitly whitelisted)

- `WPs/reports/WP16_ARCHITECT_REPORT.md` — pre-existing untracked file, left untouched per WP18 sec 1.
