# WP30 Exact Changelog

Every file created or modified on `fix/wp30-exercise6-refinements` since its base commit
`5998e04` (WP29 report/changelog, on `feature/wp29-exercise6-decision-trees`), across this
correction WP's commits.

## Created

- `WPs/WP30_EXERCISE_6_CLARITY_AND_ACTIVITY_REFINEMENTS.md` — WP30 specification (committed as
  the initial checkpoint, before any implementation file changed).
- `WPs/reports/WP30_REPORT.md` — this WP's report.
- `WPs/reports/WP30_EXACT_CHANGELOG.md` — this file.

## Modified

- `book/chapters/chapter_06/exercise_06.ipynb` (30 → 31 cells) —
  - **Sec 1 (tree diagram):** inserted a hidden (`hide-input`) helper cell defining
    `_preorder_node_ids` / `format_tree_diagram`, which post-processes `plot_tree()`'s returned
    text annotations in place (no manual image editing) to show short display-only feature
    aliases (`Left 3a thickness`, `Right area 2 thickness`), `Mean age = …` for internal nodes,
    `Leaf` / `Predicted age = …` for leaves, sample counts, ages rounded to 1 decimal, and no bare
    `value =`. Updated the figure cell to declare `FEATURE_ALIASES`, call the new formatter, add
    the title "Resulting Regression Tree", and a `fig.text` caption explaining leaf means. Updated
    the preceding markdown to name the aliases explicitly and state the real column names are
    unchanged elsewhere.
  - **Sec 2 (partition plot):** added `draw_partition_boundaries()` to the partition-plot cell —
    walks the tree, narrowing a bounding box at every split, and draws each boundary segment
    (`ax.vlines`/`ax.hlines`) clipped to its own parent node's region; only the root's split spans
    the full plot. Updated the preceding markdown to describe the gray boundary lines.
  - **Sec 3 (greedy dataset):** rewrote the "Interactive activity" intro markdown to describe the
    new simulated, overlapping two-feature dataset (removed the four-quadrant description); fully
    rewrote the optional Python-reproduction cell (still `hide-cell`) to regenerate the same 16
    observations from the committed formula/seed and reproduce the root round's candidate
    thresholds under the new variable names (`greedy_x1`, `greedy_x2`, `greedy_y`, …).
  - **Sec 4/8.2 (duplicate reflection):** deleted the "Think again, after using the activity"
    markdown cell that followed the greedy activity's iframe (near-verbatim duplicate of the
    widget's own in-iframe "Reflect" list) — see the Deviations note in the report re: scope.
  - **Sec 4 (classification contrast):** inserted a new hidden (`hide-input`) code cell,
    immediately after Section 3's regression-curve explanation/Think-First block, that runs the
    bounded classification-tree audit (Exercise 3's cohort/target coding and 360-feature recipe,
    `StratifiedKFold(5, shuffle=True, random_state=42)`, `min_samples_leaf=5`, depths 1–10,
    the same displayed-precision tie-break rule as the audit script) and plots training/validation
    ROC AUC vs. `max_depth`; followed by a new markdown cell stating the audited numbers, the
    higher-is-better/lower-is-better distinction, and the "not inherently more complex" caveat.
  - **Sec 5 (ensemble simplification):** deleted the "Think again, after using the activity"
    markdown cell that followed the ensemble activity's iframe (same duplicate pattern as Sec 4);
    updated the "One Tree or Many?" intro markdown to add a sentence naming the prediction panel's
    "fixed training sample".
  - **Sec 6 (fair comparison):** unchanged code/markdown; only its cell tag changed (see below).
  - Re-executed end-to-end (`jupyter nbconvert --execute --inplace`) to refresh every output.
- `book/downloads/chapter_06/exercise_06_portable.ipynb` (32 → 33 cells) — regenerated by
  `scripts/build_portable_notebook.py --write --notebook chapter_06` from the updated canonical
  notebook; carries the same content changes (minus the two iframes, replaced by course-website
  links as before).
- `book/config/abide_modeling.json` — added `decision_tree.classification_complexity_curve`
  (bundle/measures matching `classification.canonical_recipe`, `min_samples_leaf=5`,
  `random_state=42`, `depth_grid` 1–10, the `StratifiedKFold` spec, and a rationale note), inserted
  before `decision_tree.audit_script`; added `decision_tree.ensemble.fixed_prediction_replicate`
  (`index: 0`, `seed: 0`, rationale note) documenting the ensemble widget's fixed training sample.
- `scripts/decision_tree_model_audit.py` — added `_classification_complexity_curve()` (fits
  `DecisionTreeClassifier` at `max_depth` 1–10 under 5-fold stratified CV on the full eligible
  cohort, using the notebook's own natural column order — see the report's Corrections section —
  and evaluates the depth≥3-and-margin≥0.01 inclusion rule with the shallower-wins tie-break);
  wired it into `run_audit()` and added matching `validate()` checks (recomputes the tie-break and
  the three rule booleans, refusing a result that doesn't match); added a print-summary line.
- `scripts/decision_tree_model_audit_result.json` — regenerated by `--run`; adds the
  `classification_complexity_curve` section (n=1004, p=360, depths 1–10, per-depth train/val ROC
  AUC, `best_depth=3`, `best_val_auc=0.532069`→`0.531971`, `depth2_val_auc=0.507889`,
  `margin_over_depth2=0.024082`, `inclusion_rule.include_figure=true`); `single_tree` /
  `complexity_curve` / `fair_comparison` numbers unchanged (re-verified byte-identical modulo the
  new section and key ordering).
- `scripts/export_tree_greedy_widget.py` — replaced the four-quadrant `build_observations()` with
  the fixed generating formula (`MU1,SD1,MU2,SD2,INTERCEPT,BETA1,BETA2,GAMMA,NOISE_SD`,
  `N_OBSERVATIONS=16`) and `_generate_raw(seed)`; added `_best_and_second()` and
  `seed_diagnostics()` implementing every predeclared acceptance criterion (correlation bounds,
  cross-feature collinearity bound, root/child margin and margin-ratio bounds, minimum split
  sizes, both-features-used-somewhere) and `select_seed()` (scans `range(50)` in order); set
  `SELECTED_SEED = 17`; `build_data()` now also emits a `generatingProcess` block (formula text,
  every constant, `seedSearchRange`, `selectedSeed`, `selectionNote`) and relabels
  `features.x1/x2.label` to `Brain measure 1` / `Brain measure 2`; `validate()` now also checks
  observation count is exactly 16, recomputes `select_seed()` fresh and confirms it still picks
  17, confirms seed 17 itself passes `seed_diagnostics`, and confirms no seed `< 17` also passes.
- `book/_static/widgets/data/tree_greedy_split.json` — regenerated by `--refresh`: new 16-point
  simulated dataset (seed 17), new three-round audit (root `x1 ≤ 5.925`, left-child `x2 ≤ 5.493`,
  right-child `x2 ≤ 6.6375`), new `generatingProcess` block, relabeled feature names.
- `book/_static/widgets/configs/tree_greedy_split.json` — `description`/`instructions` reworded
  from "synthetic dataset" / "X1 or X2" to "simulated dataset" / "Brain measure 1 or Brain
  measure 2"; `reflectionPrompts` unchanged (now the activity's only reflection prompts, per the
  notebook-side markdown deletion above).
- `book/_static/widgets/configs/tree_ensemble_compare.json` — removed `defaultReplicateSeed`;
  reworded `instructions` to drop "Choose a training replicate" and describe the prediction panel
  as using "one fixed training sample".
- `interactive/src/config.ts` — removed `defaultReplicateSeed: z.number().int().nonnegative()`
  from `treeEnsembleCompareConfig`; updated its preceding comment.
- `interactive/src/tree-greedy-split-data.ts` — added a `generatingProcess` sub-schema (formula
  string, per-feature `{distribution, mean, sd}`, the five formula constants, `nObservations`,
  `seedSearchRange` tuple, `selectedSeed`, `selectionNote`) and added it as a required field of
  `treeGreedySplitDataSchema` (still `.strict()`).
- `interactive/src/components/tree-ensemble-compare.ts` — removed the replicate `<select>` (its
  label, options, `data-testid`, and change listener) and the `replicateSeed` state variable;
  added `fixedReplicate = data.replicates[0]`, used by `pointFor()` in place of
  `currentReplicate()`; updated the cohort-line and prediction-panel-heading text to describe the
  fixed training sample; shortened both MSE panels' y-axis titles to exactly `MSE (years²)` and
  added a `"MSE (years²): lower is better."` caption paragraph above each of those two panels.
- `interactive/tests/tree-greedy-split-data.test.ts` — added a `generatingProcess` block to the
  `base()` fixture (schema now requires it); added a test that a fixture missing
  `generatingProcess` is rejected.
- `interactive/e2e/tree-ensemble-compare.spec.ts` — replaced the "changing training replicate…"
  test (selector no longer exists) with a test that the replicate/seed selector and its label are
  absent; added a test that both MSE panels' Plotly `_fullLayout.yaxis.title.text` read exactly
  `MSE (years²)`.
- `interactive/e2e-book/chapter06.spec.ts` — removed the blanket `not.toContain("classification
  tree")` body-text assertion (a classification-tree panel is now legitimately present); added a
  `tree-ensemble-replicate-select` absence check to the ensemble activity's load test; changed the
  reveal-panel assertion from `toContain("x1")` to `toContain("brain measure 1")` (display alias);
  renamed the refresh-persistence test to drop "default replicate" from its title.
- `tests/test_decision_tree_model_audit.py` — added 3 tests: the classification curve's recipe/CV
  match the manifest exactly; its cohort is the full eligible cohort, not
  `classification.holdout_split`'s smaller test partition; its inclusion-rule booleans are
  reproducible from a fresh recomputation over `val_auc`.
- `tests/test_export_tree_greedy_widget_data.py` — replaced the four-quadrant-specific tests with:
  exactly-16-observations (was a `[12, 20]` range check); explicit-not-ABIDE synthetic/simulated
  wording; student-facing "Brain measure 1/2" labels; `generatingProcess` matches the script's own
  constants; every seed `< 17` fails `seed_diagnostics` (so 17 is genuinely first); both
  feature–target correlations fall in `[0.35, 0.90]`; every round's optimum strictly beats its
  runner-up with both children ≥ 3 observations. Kept: check-mode, fresh-recomputation,
  three-rounds, root-on-x1/children-on-x2, threshold-midpoint, weighted-MSE, largest-reduction,
  and partition tests (all still valid for the new dataset).
- `tests/test_exercise_06_notebook.py` — updated `CLASSIFICATION_NEEDLES` to drop
  `decisiontreeclassifier` / `classification tree` (now legitimately present) and keep only the
  logistic-regression needles; added ~20 new tests covering: diagram aliases + real-column
  preservation, `Mean age`/`Leaf`/`Predicted age` wording, a functional no-bare-`value=` check
  that executes the notebook's own formatter against a throwaway tree, a functional
  partition-boundary hierarchy check that executes `draw_partition_boundaries` against a
  throwaway tree, absence of "two-feature curve" wording, a `ClassificationComplexityContrast`
  test class tying the notebook's actual content to the committed audit's `inclusion_rule`
  (present iff it passed, correctly labeled, cohort/recipe/CV match, never touches an outer test
  set), no-quadrant / no-duplicate-reflection checks for both activities, "fixed training sample"
  wording, ensemble config has no `defaultReplicateSeed`, fair-comparison cell is `hide-input`
  (not `hide-cell`) and has a recorded output, and portable-notebook equivalents of the
  reflection/greedy-variable/fair-comparison checks. Updated the module docstring.

No files were deleted. No files were moved.
